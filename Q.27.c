/*O que será impresso depois de executado o seguinte algoritmo:
a) num = 20 == NUMERO POSITIVO
b) num = -3 == NUMERO NEGATIVO
c) num = 0 == ZERO
leia(num)
    se num > 0 então
    quale = 'NUMERO POSITIVO'
senão
    se num < 0 então
    quale = 'NUMERO NEGATIVO'
senão
    quale = 'zero';
fim se;
fim se;
escreva(quale);
*/
